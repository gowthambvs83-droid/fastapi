import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FastAPI Mastery — Zero to Production Ready",
  description: "Interactive FastAPI learning platform. From your first endpoint to production deployment — Pydantic V2, authentication, databases, WebSocket, Docker, and 5 hands-on projects.",
  keywords: ["FastAPI", "Python", "API", "Pydantic", "SQLAlchemy", "JWT", "WebSocket", "Docker", "Tutorial"],
  authors: [{ name: "FastAPI Mastery" }],
  icons: {
    icon: "data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>⚡</text></svg>",
  },
  openGraph: {
    title: "FastAPI Mastery — Zero to Production Ready",
    description: "The complete guide to building real-world APIs with Python",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
